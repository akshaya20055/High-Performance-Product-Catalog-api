DATABASE_URL=postgresql+asyncpg://neondb_owner:npg_vXJVTzh50Bnf@ep-divine-feather-aoro4lt5.c-2.ap-southeast-1.aws.neon.tech/neondb

# Application Configuration
ENV=development
HOST=0.0.0.0
PORT=8000
LOG_LEVEL=info

# Testing Configuration
# Used for running pytest (uses a separate database or sqlite in-memory for unit tests)
TEST_DATABASE_URL=sqlite+aiosqlite:///:memory:


# Application Configuration
ENV=development
HOST=0.0.0.0
PORT=8000
LOG_LEVEL=info

# Testing Configuration
# Used for running pytest (uses a separate database or sqlite in-memory for unit tests)
TEST_DATABASE_URL=sqlite+aiosqlite:///:memory:
